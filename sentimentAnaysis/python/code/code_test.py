import stop_words as stp
import lemmatize_words as lem
#import pickle
#twitterData = sys.argv[1] # tweet input file (.csv)
print(stp.stop_words("I went canouing.  Lake last night in the darkened countryside. But this was not the first time. So at my earliest I did it again."))
#print(stp.stop_words("I went canouing.  Lake last night in the darkened countryside. But this was not the first time. So at my earliest I did it again."))

